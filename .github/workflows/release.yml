on:
  push:
    branches:
      - master
    tags:
      - "v*"

name: ytdl-release

jobs:
  build:
    name: Release and Upload Asset
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Pack
        run: |
          npm install
          npm run pack

      - name: Check version change
        uses: thebongy/version-check@v1
        id: check
        with:
          file: package.json

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ steps.check.outputs.releaseVersion }}
          release_name: Release v${{ steps.check.outputs.releaseVersion }}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./dist/cli/ytdl
          asset_name: ytdl
          asset_content_type: text/plain
